<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows cleartext (HTTP) traffic to localhost and the Android emulator's
  host-loopback alias. Used during development to reach the backend running
  on the developer machine (via `adb reverse tcp:8000 tcp:8000` on a physical
  device, or 10.0.2.2 on the emulator). Remove/tighten before any real deploy.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">192.168.1.5</domain>
    </domain-config>
</network-security-config>
