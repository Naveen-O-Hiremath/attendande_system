/// Backend base URL.
///
/// Set to the development machine's LAN IP so a phone on the same Wi-Fi can
/// reach it directly (no USB/adb reverse required). Update this if the
/// machine's IP changes networks.
const String apiBaseUrl = 'http://192.168.1.5:8000/api/v1';
